name: CI/CD Security Scan

on:
  schedule:
    - cron: '50 * * * *'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scan schedule triggered at $(date -u)"
